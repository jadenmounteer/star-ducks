// The icon of the starship used on the course plotter map
export interface StarshipIcon {
  coordinates: { x: number; y: number };
  sprite: string;
  animationFrames: number;
  size: number;
  speed: number;
}
